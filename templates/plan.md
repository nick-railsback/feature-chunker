# Chunk <nn>-<slug> — plan

**Approach:** <the how, in a paragraph. Cold-executable standard: an agent
that has read nothing but spec.md, this file, and the red tests can execute.>

**Red demonstration:** `oracle-red.log` in this chunk directory, written by
`chunk-check.sh freeze` when it executes `oracle_cmd` and requires red.

## Steps

1. <ordered, concrete>
2. <...>

## Files to touch

Must be a subset of spec.md declared scope.

- <path>

## Risks & stop-and-surface triggers

- <what would mean this plan is wrong; implementer stops and surfaces>

## Deviations (implementer appends during implement)

- <date> <in-scope deviation taken and why>

## Handoff note (implementer, at exit)

<what was built; observations for the retro; candidate follow-up chunks>
